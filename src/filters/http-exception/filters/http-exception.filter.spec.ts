import { Filters\httpExceptionFilter } from './filters\http-exception.filter';

describe('Filters\httpExceptionFilter', () => {
  it('should be defined', () => {
    expect(new Filters\httpExceptionFilter()).toBeDefined();
  });
});
